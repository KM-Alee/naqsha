# Copy to `.env` and fill in locally. Never commit real secrets.
# Run Profiles reference these names via `api_key_env` — not secret values.

# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=

# Opt-in automated live-network pytest (paid APIs):
# NAQSHA_LIVE=1
